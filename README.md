# AI-Mektep

Monorepo: React + Node.js + PostgreSQL

## Quick start

1. `docker-compose up -d`
2. `npm install`
3. `npm run db:migrate`
4. `npm run import:drive` (optional, after Google Drive creds setup)
5. `npm run dev`

Frontend: `http://localhost:5173`
Backend: `http://localhost:3000`
